# Auth.js (NextAuth v5)
AUTH_SECRET=  # 运行 npx auth secret 生成

# Prisma / Supabase PostgreSQL（双连接，从 Dashboard Connect 复制）
# DATABASE_URL：Transaction 模式 (6543)，应用运行时
# DIRECT_URL：Session 模式 (5432)，prisma migrate 必须用此
DATABASE_URL=
DIRECT_URL=
