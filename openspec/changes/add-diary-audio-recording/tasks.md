## 1. 数据模型与持久化准备

- [x] 1.1 在 `prisma/schema.prisma` 中为 `DiaryEntry` 增加 `audioUrl`、`audioName`、`audioDurationSec`、`audioMimeType` 可选字段，并生成迁移。
- [x] 1.2 扩展日记读取 DTO 与相关类型，使创建、更新、读取日记时支持单段录音元数据。
- [x] 1.3 扩展 `src/app/store.ts` 中的 `DiaryEntry`、`saveEntry`、`addEntry`、`updateEntry` 类型与参数透传逻辑。
- [x] 1.4 新增基于 `FormData` 的音频上传入口与存储层音频上传封装，将单段录音二进制直传到对象存储。
- [x] 1.5 接通日记保存流程与音频上传入口，使前端在保存时可先上传音频文件，再持久化日记中的音频 URL 与元数据。

## 2. 共享录音与确认组件

- [x] 2.1 新增 `AudioRecorderField` 组件，封装开始录音、停止录音、120 秒自动停止、实时声波、试听与重录逻辑。
- [x] 2.2 在录音组件中使用 `Blob + URL.createObjectURL()` 管理本地预览，并在重录、删除和卸载时释放对象 URL。
- [x] 2.3 在录音组件中支持展示名称编辑，并明确展示名称只影响业务元数据不影响真实存储路径。
- [x] 2.4 抽取通用确认弹窗基座，支持注入标题、描述、确认按钮文案与危险操作状态。
- [x] 2.5 将现有删除日记本弹窗迁移为复用通用确认弹窗基座，保持现有视觉风格不变。
- [x] 2.6 基于通用确认弹窗实现删除录音确认交互，并在确认后清空录音与名称状态。

## 3. 首页创建页接入

- [x] 3.1 在 `src/app/pages/Dashboard.tsx` 中接入录音表单状态，并将录音按钮放在添加图片按钮左侧。
- [x] 3.2 调整首页输入区布局顺序为“正文区域 -> 图片列表 -> 录音内容区”。
- [x] 3.3 将录音结果与图片、文本一起纳入首页保存逻辑，并处理“录音中禁止保存”等边界状态。

## 4. 详情页创建与编辑接入

- [x] 4.1 在 `src/app/pages/DiaryView.tsx` 的新建态与编辑态接入录音表单状态。
- [x] 4.2 将录音区放置在左页图片区下方，并在只读态隐藏录音录制控件。
- [x] 4.3 让详情页的新建保存与编辑更新都支持录音元数据透传、重录与删除确认。
- [x] 4.4 在只读展示区域与 `ViewEntryModal` 中补充录音名称与音频播放器展示。

## 5. 联调、回归与验证

- [x] 5.1 为 Server Action 或相关数据层补充测试，覆盖录音字段的创建、更新与读取。
- [x] 5.2 为前端关键交互补充测试或手动验证清单，覆盖 120 秒自动停止、删除确认取消/确认、展示名称保存。
- [x] 5.3 回归验证首页与详情页原有文本、图片、日记本删除能力未受影响。
- [x] 5.4 运行项目相关检查命令，确保类型、测试与页面交互可正常工作。
